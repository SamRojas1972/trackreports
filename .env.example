# Ejemplo de archivo de variables de entorno
# Copiar a .env y configurar con valores reales

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trayectoria_db
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_SCHEMA=core
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# Rutas del proyecto
PROJECT_ROOT=/ruta/a/trayectoriacode

# Configuración de la aplicación
ENVIRONMENT=development  # development, staging, production
DEBUG=true
